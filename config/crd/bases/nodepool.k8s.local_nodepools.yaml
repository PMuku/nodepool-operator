---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: nodepools.nodepool.k8s.local
spec:
  group: nodepool.k8s.local
  names:
    kind: NodePool
    listKind: NodePoolList
    plural: nodepools
    singular: nodepool
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: NodePool is the Schema for the nodepools API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NodePool
            properties:
              label:
                description: Label applied to nodes in the pool (key=value)
                pattern: ^[a-zA-Z0-9_.-]+=[a-zA-Z0-9_.-]+$
                type: string
              nodeSelector:
                additionalProperties:
                  type: string
                description: NodeSelector defines required labels for eligible nodes
                type: object
              size:
                description: NodePool size
                format: int32
                minimum: 0
                type: integer
              taint:
                description: Taint applied to nodes in the pool (key=value:effect)
                type: string
            required:
            - label
            - size
            type: object
          status:
            description: status defines the observed state of NodePool
            properties:
              assignedNodes:
                items:
                  type: string
                type: array
              currentSize:
                format: int32
                type: integer
              desiredSize:
                format: int32
                type: integer
              message:
                type: string
              phase:
                description: Phase indicates the overall health of the NodePool
                enum:
                - Ready
                - Pending
                - Degraded
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
